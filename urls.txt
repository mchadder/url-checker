# Format is Expected HTTP response code component of URL
# e.g. 200 http://server:port/uri [curl config file]
# bash brace expansion is supported as the example below shows.
404 http://www.google.co.uk/{1,2,3,4,5,6} curl.cfg

